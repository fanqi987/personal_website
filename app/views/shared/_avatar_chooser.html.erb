<% if object.user_id %>
  <%= image_tag "pokemon/#{User.find_by(id: object.user_id).avatar}" %>
<% elsif object.avatar && !object.avatar.empty? %>
  <%= image_tag "pokemon/#{object.avatar}" %>
<% else %>
  <%= image_tag "pokemon/0" %>
<% end %>